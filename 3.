class CuentaBancaria:
    def __init__(self, titular, saldo, numero_cuenta):
        self._titular = titular
        self._saldo = saldo
        self._numero_cuenta = numero_cuenta

    def depositar(self, monto):
        if monto > 0:
            self._saldo += monto

    def retirar(self, monto):
        if 0 < monto <= self._saldo:
            self._saldo -= monto
        else:
            print("Fondos insuficientes")

    def get_saldo(self):
        return self._saldo
        
class CuentaBancaria:
    def __init__(self, titular, saldo, numero_cuenta):
        self._titular = titular
        self._saldo = saldo
        self._numero_cuenta = numero_cuenta

    def depositar(self, monto):
        if monto > 0:
            self._saldo += monto

    def retirar(self, monto):
        if 0 < monto <= self._saldo:
            self._saldo -= monto
        else:
            print("Fondos insuficientes")

    def get_saldo(self):
        return self._saldo


cuenta = CuentaBancaria("Adolf", 1000, "1945")

cuenta.depositar(500)
cuenta.retirar(200)
cuenta.retirar(2000)

print("Saldo final:", cuenta.get_saldo())
